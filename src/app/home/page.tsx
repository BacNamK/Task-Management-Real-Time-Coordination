import { signOut } from '@/src/lib/auth';
import { Metadata } from 'next';

export const metadata: Metadata = {
    title: 'Home',
};

const Page = async () => {
    return <div></div>;
};

export default Page;
